<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:paddingHorizontal="8dp"
    android:paddingVertical="16dp">

    <!-- 헤더 섹션 -->
    <TextView
        android:id="@+id/tv_why_rise_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="왜 올랐을까?"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_total_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="총 23회"
        android:textColor="#888888"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/tv_why_rise_title"
        app:layout_constraintStart_toEndOf="@id/tv_why_rise_title"
        app:layout_constraintTop_toTopOf="@id/tv_why_rise_title" />

    <ImageButton
        android:id="@+id/btn_info"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="정보"
        android:src="@drawable/ic_question_mark"
        android:tint="#CCCCCC"
        app:layout_constraintBottom_toBottomOf="@id/tv_why_rise_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_why_rise_title" />

    <!-- 리스트 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_why_rise_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:nestedScrollingEnabled="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_why_rise_title"
        tools:listitem="@layout/item_why_rise" />

</androidx.constraintlayout.widget.ConstraintLayout> 